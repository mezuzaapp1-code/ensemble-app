[build]
builder = "docker"

[deploy]
restartPolicyType = "ON_FAILURE"
# Do not wrap in single quotes — $PORT would not expand and health checks fail.
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
